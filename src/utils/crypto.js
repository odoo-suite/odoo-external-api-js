// In this file the data encryption functionality has been removed.
// If you want to implement a process to encrypt the JWT you can implement it here.

const crypto_encrypt = (str) => {
  return str;
};

const crypto_decrypt = (str) => {
  return str;
};

module.exports = { crypto_encrypt, crypto_decrypt };
